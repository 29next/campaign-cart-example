{
  "name": "campaign-cart-example",
  "version": "1.0.0",
  "description": "Example Campaign using Campaign Cart SDK",
  "scripts": {
    "setup": "campaign-init",
    "start": "campaign-dev",
    "dev": "campaign-dev",
    "clone": "campaign-clone",
    "config": "campaign-config",
    "build": "eleventy",
    "compress": "campaign-compress",
    "compress:preview": "campaign-compress --preview"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "next-campaign-page-kit": "^0.0.3"
  }
}
